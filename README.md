## This Project is just for learning.
## Can be used for practical applications.

### Setup
1. Create a `.env` file with `VITE_EXCHANGE_RATE_API_KEY`.
2. Run `npm install` and `npm run dev`.

If the API key is missing, the app automatically falls back to the free, keyless endpoint at `open.er-api.com`.
